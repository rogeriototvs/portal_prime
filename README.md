# Portal Prime

Aplicação web (Vite + React + TypeScript + Tailwind + shadcn/ui) com integração ao Supabase.

## Rodar localmente

```bash
bun install
bun run dev
```

## Build

```bash
bun run build
```
